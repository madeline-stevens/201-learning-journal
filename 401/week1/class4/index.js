'use strict';

const Bitmap = require('./lib/bitmap');
const fs = require('fs')

//bitmap will be module b that's aquired into module a. how can i isolate one thing to kick it off to isolate one or two other things and kick them off...???

fs.readFile()
